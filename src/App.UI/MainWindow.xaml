<Window x:Class="App.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:App.UI.ViewModels"
        mc:Ignorable="d"
        Title="Doc Medic - Document Formatter &amp; Hyperlink Fixer" 
        Height="800" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.DataContext>
        <viewModels:MainWindowViewModel />
    </Window.DataContext>

    <materialDesign:DialogHost CloseOnClickAway="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <!-- Navigation Panel -->
                <ColumnDefinition Width="250" />
                <!-- Main Content Area -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Navigation Panel -->
            <Grid Grid.Column="0" Background="{DynamicResource MaterialDesignCardBackground}">
                <Grid.RowDefinitions>
                    <!-- App Header -->
                    <RowDefinition Height="Auto" />
                    <!-- Navigation Items -->
                    <RowDefinition Height="*" />
                    <!-- Theme Toggle -->
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- App Header -->
                <StackPanel Grid.Row="0" Margin="16" TextBlock.Foreground="{DynamicResource MaterialDesignBody}">
                    <materialDesign:PackIcon Kind="FileDocument" Width="32" Height="32" 
                                           HorizontalAlignment="Center" Margin="0,8" />
                    <TextBlock Text="Doc Medic" FontSize="18" FontWeight="Medium" 
                             HorizontalAlignment="Center" />
                    <TextBlock Text="v1.0.0" FontSize="12" Opacity="0.7" 
                             HorizontalAlignment="Center" />
                </StackPanel>

                <!-- Navigation Items -->
                <StackPanel Grid.Row="1" Margin="8,16,8,8">
                    <RadioButton Style="{StaticResource NavItemStyle}"
                               Content="Process Files" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="HomePage"
                               IsChecked="True">
                        <RadioButton.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileMultiple" 
                                                           VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </RadioButton.ContentTemplate>
                    </RadioButton>

                    <RadioButton Style="{StaticResource NavItemStyle}"
                               Content="Settings" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="SettingsPage">
                        <RadioButton.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Settings" 
                                                           VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </RadioButton.ContentTemplate>
                    </RadioButton>

                    <RadioButton Style="{StaticResource NavItemStyle}"
                               Content="Logs" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="LogsPage">
                        <RadioButton.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="TextBox" 
                                                           VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </RadioButton.ContentTemplate>
                    </RadioButton>

                    <RadioButton Style="{StaticResource NavItemStyle}"
                               Content="About" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="AboutPage">
                        <RadioButton.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Information" 
                                                           VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </RadioButton.ContentTemplate>
                    </RadioButton>
                </StackPanel>

                <!-- Theme Toggle -->
                <StackPanel Grid.Row="2" Margin="16" Orientation="Horizontal" HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="WeatherSunny" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                IsChecked="{Binding IsDarkTheme}" />
                    <materialDesign:PackIcon Kind="WeatherNight" VerticalAlignment="Center" Margin="8,0,0,0" />
                </StackPanel>
            </Grid>

            <!-- Main Content Frame -->
            <Grid Grid.Column="1" Style="{StaticResource MainContentStyle}">
                <ContentControl prism:RegionManager.RegionName="ContentRegion" />
            </Grid>
        </Grid>
    </materialDesign:DialogHost>
</Window>
